<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.madeinbrasil">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.PeopleActivity"></activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.madeinbrasil.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <activity android:name=".view.activity.SelectActivity"></activity>
        <activity android:name=".view.activity.SplashActivity"></activity>
        <activity android:name=".view.activity.ListDetailActivity" />
        <activity android:name=".view.activity.AchievementActivity"></activity>
        <activity android:name=".view.activity.MyProfileOptionsActivity"></activity>
        <activity android:name=".view.activity.FilmsAndSeriesActivity" />
        <activity android:name=".view.activity.MenuActivity">
        </activity>
        <activity android:name=".view.activity.RegisterActivity" />
        <activity android:name=".view.activity.LogInActivity" />
        <activity android:name=".view.activity.InitialActivity"></activity>
        <activity android:name=".view.activity.CreateListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".view.activity.FriendsActivity"></activity>
        <activity android:name=".view.activity.UserActivity">
        </activity>
    </application>

</manifest>